#include "Node.c"
//Reversing elements and Reversing Links
int reverse(struct node * p)
{
    struct node * q;
    q =  (struct node *)malloc(sizeof(struct node));
    while(p!=0)
    {
        p = p->next;   
    }
}